[project]
name = "ariadne-evaluation"
version = "0.1.0"
description = "Metrics + benchmark runners for ARIADNE (no fabricated numbers)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["ariadne_evaluation"]
